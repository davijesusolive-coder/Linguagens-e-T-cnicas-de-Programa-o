#include <stdio.h>
#include <stdlib.h>
int main(int argc, char *argv[]) {
	float base = 5, altura = 10, area;
	
	area = (base * altura) / 2;
	
	printf("A area do triangulo e: %.2f\n", area);
	return 0;
}
